apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-nginx-a
  namespace: demo-a
spec:
  podSelector:
    matchLabels:
      app: nginx-a
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: demo-a # ← fixed label
      ports:
        - protocol: TCP
          port: 80
