apiVersion: kind.x-k8s.io/v1alpha4
kind: Cluster

nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30000
        hostPort: 30000

  - role: worker
  - role: worker

networking:
  disableDefaultCNI: true
  podSubnet: 192.168.0.0/16
